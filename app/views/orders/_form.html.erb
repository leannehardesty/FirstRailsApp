<%= form_for(@orders) do |f| %>
  <% if @orders.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orders.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @orders.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="newborder">
    <div class="field">
      <%= f.label :user_id %><br>
      <%= f.int :user_id %>
    </div>
    <div class="field">
      <%= f.label :product_id %><br>
      <%= f.int :product_id %>
    </div>
    <div class="field">
      <%= f.label :total %><br>
      <%= f.text_field :total %>
    </div>
    

    <div class="actions">
      <%= f.submit %>
    </div>

  </div>
<% end %>